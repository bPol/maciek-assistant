{
  "name": "@maciek/console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "maciek": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "lint": "echo \"(no lint configured)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@maciek/agents": "*"
  }
}
